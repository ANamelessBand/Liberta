  h3.mt-5 Просрочени копия
  table.table.table-striped
    thead
      th Потребител
      th Публикация
      th Дата на връщане
      th Действия
    tbody
      - if @overdue.empty?
        tr
          td(colspan="4") няма просрочени копия
      - @overdue.each do |copy|
        tr
          td= link_to copy.current_loan.user.email, copy.current_loan.user
          td= link_to copy.print.title, copy.print
          td.text-danger= copy.current_loan.time_supposed_return.strftime("%B %e, %Y")
          td
            = link_to print_copy_path(copy.print, copy), class: 'btn btn-xs btn-outline-primary' do
              - fa_icon 'share', text: 'към копието'
            = link_to extend_print_copy_loan_path(copy.print, copy, copy.current_loan),
                method: :post,
                class: 'btn btn-xs btn-outline-primary ml-2' do
              - fa_icon 'mail-forward', text: 'още 7 дни'
            = link_to return_print_copy_loan_path(copy.print, copy, copy.current_loan),
                method: :post,
                class: 'btn btn-xs btn-outline-success ml-2' do
              - fa_icon 'refresh', text: 'върни'

  h3.mt-5 Желани публкации
  table.table.table-striped
    thead
      th Потребител
      th Публикация
      th Свободни копия
    tbody
      - if @wishlists.empty?
        tr
          td(colspan="3") няма желани публикации
      - @wishlists.each do |wishlist|
        tr
          td= link_to wishlist.user.email, wishlist.user
          td= link_to wishlist.print.title, wishlist.print
          td= wishlist.print.free_copies.count
